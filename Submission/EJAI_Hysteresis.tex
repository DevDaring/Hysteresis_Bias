%% Measurement-validity study of bias injection/removal asymmetry.
%% Target: The European Journal on Artificial Intelligence (SAGE).
%% Compiles with pdflatex; references via SageH.bst (Harvard author-year).
\documentclass[Afour,sageh,times]{sagej}

\usepackage{booktabs}
\usepackage{adjustbox}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algpseudocode}

\setcounter{secnumdepth}{3}

\begin{document}

%% ANONYMIZED for double-anonymized peer review.
%% Author, affiliation, corresponding-author, and contact details are on the
%% separate TitlePage_EJAI_Hysteresis.tex, which is withheld from reviewers.
\runninghead{Step-count ratios do not measure bias hysteresis}

\title{Step-count ratios do not measure bias hysteresis: a matched-objective control for multilingual encoder models}

\author{Anonymous}

\affiliation{}

\begin{abstract}
Debiasing pipelines often assume that removing a social bias costs about as much as introducing it. Recent work reports the opposite, a bias hysteresis in which removal needs more gradient steps than injection, summarised by the step-count ratio $R = T_{\mathrm{debias}}/T_{\mathrm{bias}}$. This paper shows the ratio does not measure hysteresis. On one grid of five multilingual encoders and three languages, $R$ is computed under the original objective and under a matched objective that drives the same log-probability gap both ways. With censored conditions excluded and the median replacing the mean, removal is the faster direction. The ratio also swings by more than an order of magnitude as the threshold moves, so it cannot be a stable property of the model. A threshold-free loop-area measure finds only a small path dependence, near one to two per cent of full scale. Asymmetry claims need a matched objective, censoring-aware analysis, and a threshold-free measure.
\end{abstract}

\keywords{Language model bias, multilingual fairness, bias mitigation, measurement validity, responsible AI}

\maketitle

\section{Introduction}
\label{sec:introduction}

Social biases in language models raise concern as these models enter hiring tools, content moderation, and education \citep{blodgett2020language}. Stereotypical associations learned during pretraining carry into downstream use and can amplify existing inequalities \citep{caliskan2017semantics}. A large body of work detects such biases and proposes methods to remove them \citep{bolukbasi2016man, ravfogel2020null, schick2021selfdiagnosis}.

Most of this work assumes that the effort to remove a bias is comparable to the effort to introduce it. A model pushed towards a stereotype over a fixed number of gradient steps is expected to recover over a similar number of steps. Recent studies question that assumption. They inject a bias and then remove it, to study acquisition and unlearning as distinct processes \citep{islam2025biasgym, liu2025rethinking}. A natural summary of such an inject-then-remove protocol is the step-count ratio $R = T_{\mathrm{debias}} / T_{\mathrm{bias}}$, where $T_{\mathrm{bias}}$ is the step count to push a bias score above a threshold and $T_{\mathrm{debias}}$ is the step count to bring it back below. When removal takes more steps than injection, so that $R > 1$, the protocol is read as showing an asymmetry between acquisition and unlearning. By analogy with physical systems whose state depends on history, this asymmetry is called bias hysteresis.

Figure~\ref{fig:curves} shows what such a run looks like for the five encoder models studied here. In each panel the red line is injection: as training adds stereotypical examples, the bias score climbs above the dashed threshold. The blue line is removal: from the biased model, debiasing pulls the score back down below the threshold. The score rises and then falls, which is the shape a hysteresis claim rests on. Removal reaches the threshold in fewer steps than injection took, so removal is already the faster direction here. The rest of the paper asks whether the step-count ratio built from these two curves means what it appears to mean.

\begin{figure*}[htbp]
\centering
\includegraphics[width=\textwidth]{images/figure_hysteresis_curves.png}
\caption{Injection (red) and removal (blue) bias-score curves for the five encoder models, under the matched objective, for one representative condition each. The dashed line marks the threshold; the dotted line marks the switch from injection to removal. The score rises during injection and falls during removal, and removal reaches the threshold in fewer steps than injection took.}
\label{fig:curves}
\end{figure*}

The ratio carries two measurement problems that its interpretation overlooks. The first is censoring. When injection does not reach the threshold within its step budget, $T_{\mathrm{bias}}$ is capped; when removal does not return below the threshold, $T_{\mathrm{debias}}$ is capped. A capped ratio is a right-censored lower bound, not a measurement, yet a mean taken over such values treats the cap as a real number and pulls the average up. The second is the objective. Injection and removal are often optimised with different loss functions, one using masked-language-model cross-entropy on stereotypical targets and the other a squared difference between stereotypical and anti-stereotypical log-probabilities. These losses differ in gradient scale, so a step under one is not comparable to a step under the other.

This study tests the asymmetry on one grid under two objectives. The mismatched objective reproduces the original protocol, with cross-entropy injection and squared-gap removal. The matched objective optimises the same signed log-probability gap in both directions, so the two paths differ only in sign. Every other setting is held fixed: the same optimiser, learning rate, low-rank adapter configuration, and evaluation schedule. Crossing times are estimated by interpolation and censored cases are excluded rather than capped, and the median replaces the mean. Both objectives are evaluated across five multilingual encoder models, three languages, four bias categories, and three seeds, with gradient norms recorded so that any residual asymmetry stays visible.

This paper makes three contributions. It introduces a matched-objective, censoring-aware protocol for injection--removal asymmetry and applies it on the same grid as the original mismatched protocol, across five multilingual encoders and three languages. It shows that the reported ratio above one does not survive proper analysis, and that the ratio swings by more than an order of magnitude as the threshold moves, so it cannot be a stable property of the model. It measures bias hysteresis directly with a threshold-free loop area, which finds only a small, model-dependent path dependence, far below the large asymmetry the ratio implied.

\section{Related work}
\label{sec:related}

Bias in language models has been studied since word-embedding work showed that distributional semantics absorb human stereotypes \citep{caliskan2017semantics, bolukbasi2016man}. The concern carries into contextual models. Benchmarks such as StereoSet \citep{nadeem2021stereoset}, CrowS-Pairs \citep{nangia2020crowspairs}, and Indian-BhED \citep{khandelwal2023indianbhed} measure stereotypical preference across categories and languages. \citet{kaneko2022unmasking} proposed the Average Unmasked Likelihood to avoid frequency effects in masked-language-model scoring. \citet{blodgett2021stereotyping} showed that several pair-based benchmarks contain confounds, which cautions against reading any single derived number as a clean signal.

Debiasing methods span several mechanism classes. Counterfactual Data Augmentation retrains on swapped demographic terms \citep{zmigrod2019counterfactual}. Iterative Nullspace Projection removes bias directions from representations \citep{ravfogel2020null}. Self-Debias adjusts decoding without weight updates \citep{schick2021selfdiagnosis}. Weight-editing methods such as DAMA \citep{limisiewicz2024dama} and BiasEdit \citep{xu2025biasedit} modify targeted parameters directly. \citet{meade2022empirical} compared these methods and found that measured effectiveness depends heavily on the evaluation setup, which again points to measurement sensitivity.

The idea that removal costs more than injection grows out of two recent lines of work. \citet{islam2025biasgym} present a framework that first injects a bias into a model and then removes it, and that identifies the components involved. \citet{liu2025rethinking} study machine unlearning and observe that removing learned content can cost more than adding it. Both treat injection and removal as separate processes and count the work each takes. A natural way to summarise such an inject-then-remove run is the step-count ratio. The common instantiation trains injection with masked-language-model cross-entropy on the stereotypical target and trains removal with a squared log-probability gap. These two directions use different losses, and the run stops each direction at a fixed step budget, so the ratio inherits both the loss mismatch and the censoring described in Section~\ref{sec:introduction}. Neither line of work reports whether the asymmetry survives a matched objective or a censoring-aware summary.

Concern about how bias is measured is not new for static scores. \citet{goldfarbtarrant2021intrinsic} showed that intrinsic bias scores do not predict downstream bias, so a clean-looking number need not track the effect it is meant to capture. \citet{delobelle2022measuring} found that fairness numbers move with the probing choice, and \citet{meade2022empirical} found that the measured success of a debiasing method depends heavily on the evaluation setup. \citet{blodgett2021stereotyping} showed that widely used pair benchmarks contain confounds. Together these results argue that a derived bias number should be read with care about how it was produced. This paper carries that argument from a static score to a dynamic quantity, the step-count ratio, and asks whether the ratio measures what its name claims.

\section{Method}
\label{sec:methodology}

Measuring an asymmetry between two training directions requires that the two directions be comparable. This section first defines the bias score and the ratio, then describes the matched objective that makes the comparison fair.

\subsection{Models, data, and bias score}

The study evaluates five multilingual encoder models: mBERT \citep{devlin2019bert}, XLM-RoBERTa \citep{conneau2020unsupervised}, MuRIL \citep{khanuja2021muril}, IndicBERTv2 \citep{doddapaneni2023indicbert}, and mmBERT \citep{mmbert2025}. All five support English, Hindi, and Bengali. Each model is fine-tuned with low-rank adapters \citep{hu2022lora} of rank 16, so that injection and removal modify the same small parameter set and start from the same pretrained weights.

Bias is drawn from two benchmarks. Multi-CrowS-Pairs \citep{nangia2020crowspairs} provides sentence pairs across Western-framed categories, and an Indian-context set derived from Indian-BhED \citep{khandelwal2023indianbhed} adds caste, gender, religion, and race. Both are extended to Hindi and Bengali through translation with back-translation checks. The study reports on four categories with adequate sample counts: race-colour, religion, nationality, and socioeconomic status. Each item is a masked sentence with a stereotypical and an anti-stereotypical target, for example \textit{All the dirty and petty jobs are done by} \texttt{[MASK]}, with targets \textit{Dalits} and \textit{Brahmins}. Parallel Hindi and Bengali items exist for every English item in the caste, gender, religion, and race categories.

The bias score uses the Average Unmasked Likelihood \citep{kaneko2022unmasking}. For one item, the model reads the sentence with the target position masked and returns a log-probability for the stereotypical target and one for the anti-stereotypical target. The item score is
\begin{equation}
B = \sigma\!\left(\log P_{\mathrm{stereo}} - \log P_{\mathrm{anti}}\right),
\label{eq:bias}
\end{equation}
where $\sigma$ is the logistic function that maps the gap to the range $[0,1]$. A score above $0.5$ means the model prefers the stereotypical target. The bias of a model on a set of items is the mean of $B$ over the set. This score is convenient here because the same log-probability gap that defines it also drives the matched objective in Section~\ref{sec:methodology}. The threshold $\theta = 0.7$ marks a clear stereotypical preference: the model favours the stereotypical target with a probability near seven in ten.

\subsection{The asymmetry ratio}

Bias injection trains the model until its bias score first exceeds a threshold $\theta$. The step count at that crossing is $T_{\mathrm{bias}}$. Bias removal then trains from the injected checkpoint until the score falls back below $\theta$, giving $T_{\mathrm{debias}}$. The asymmetry ratio is
\begin{equation}
R = \frac{T_{\mathrm{debias}}}{T_{\mathrm{bias}}},
\label{eq:R}
\end{equation}
where $R > 1$ means removal takes more steps than injection. The threshold is set to $\theta = 0.7$. The bias score is read every 25 steps, so a crossing falls between two evaluations. Let $(s_0, B_0)$ and $(s_1, B_1)$ be the two evaluations that bracket $\theta$. The crossing step is estimated by linear interpolation,
\begin{equation}
T = s_0 + \frac{\theta - B_0}{B_1 - B_0}\,(s_1 - s_0),
\label{eq:cross}
\end{equation}
so $T$ is continuous rather than snapped to the evaluation grid. Injection runs for at most 500 steps and removal for at most 1{,}000 steps. If no pair of evaluations brackets $\theta$ within that budget, the crossing is censored: the true step count is only known to be larger than the budget, so the condition carries no defined ratio.

\subsection{Two objectives on one grid}

The ratio $R$ is computed under two objectives on the identical grid. The mismatched objective reproduces the original protocol: injection minimises masked-language-model cross-entropy on the stereotypical target, and removal minimises the squared gap $(\log P_{\mathrm{stereo}} - \log P_{\mathrm{anti}})^2$. The matched objective replaces both with one functional. For a batch of size $N$, let
\begin{equation}
\delta = \frac{1}{N}\sum_{i=1}^{N}\left(\log P_{\mathrm{stereo}}^{(i)} - \log P_{\mathrm{anti}}^{(i)}\right),
\label{eq:delta}
\end{equation}
the mean signed gap between the log-probability of the stereotypical target and that of the anti-stereotypical target at the masked position. Injection minimises $-\delta$, which widens the gap; removal minimises $+\delta$, which closes it. The two directions then differ only in sign. Both objectives share one optimiser, one learning rate of $2\times10^{-4}$, one adapter configuration, and one schedule, so the mismatched and matched runs differ only in the loss used.

The gradient $L_2$ norm is recorded at every step in both directions. A systematic difference between the directions exposes any residual asymmetry that the shared objective does not remove. The grid contains five encoders, three languages, four categories, and three seeds, for 180 conditions per objective. Two rules govern undefined ratios. Where a model already scores at or above $\theta$ before injection, the injection cost is undefined and the condition is excluded. Where injection does not reach $\theta$ within its budget, the condition is right-censored and also excluded, rather than assigned the ceiling value $R = T_{\mathrm{debias}}^{\max}/T_{\mathrm{bias}}^{\max}$ that the original mean aggregation would use.

Summaries report the median rather than the mean, because the median is not moved by a few extreme values the way the mean is. The confidence interval on the median comes from a bootstrap that resamples at the model level: models are drawn with replacement, their conditions are pooled, and the median of the pool is recorded, repeated ten thousand times. Resampling whole models rather than single conditions respects the nesting of conditions within a model and gives an honest interval. Algorithm~\ref{alg:main} states the full measurement loop for one condition under one objective.

\begin{algorithm}[htbp]
\caption{Inject, remove, and measure $R$ for one condition}
\label{alg:main}
\begin{algorithmic}[1]
\State \textbf{Input:} model, item set, threshold $\theta$, objective (matched or mismatched)
\State attach a fresh low-rank adapter; measure baseline bias $B_0$
\If{$B_0 \ge \theta$} \State \textbf{return} undefined (already biased) \EndIf
\State \textbf{Injection:} train the adapter to widen the gap; every 25 steps measure $B$
\State stop when $B \ge \theta$ or the budget ends; set $T_{\mathrm{bias}}$ by Eq.~\eqref{eq:cross}, or censor
\If{injection censored} \State \textbf{return} undefined (censored) \EndIf
\State \textbf{Removal:} train the adapter to close the gap; every 25 steps measure $B$
\State stop when $B \le \theta$ or the budget ends; set $T_{\mathrm{debias}}$ by Eq.~\eqref{eq:cross}, or censor
\State \textbf{return} $R = T_{\mathrm{debias}} / T_{\mathrm{bias}}$
\end{algorithmic}
\end{algorithm}

\section{Results}
\label{sec:results}

Proper analysis removes the asymmetry under both objectives. The analysis first compares the two objectives on the grid, then shows that censoring accounts for the earlier result, then break the matched objective down by model and language, and finally examine the gradient norms that set the direction of the effect.

\subsection{Neither objective shows the asymmetry}

Table~\ref{tab:ab} compares the two objectives on the same 180 conditions. Under the matched objective, 168 conditions yield a defined ratio and the median $R$ is 0.393, with a model-clustered 95\% confidence interval of [0.359, 0.426]. Under the mismatched objective, 119 conditions yield a defined ratio and the median $R$ is 0.074, with an interval of [0.056, 0.080]. Both intervals lie well below one. A one-sample Wilcoxon signed-rank test on $\log R$ rejects symmetry under each objective ($p = 4\times10^{-15}$ matched, $p = 2\times10^{-19}$ mismatched), and both medians sit below one, so both reject in the direction $R < 1$. Figure~\ref{fig:rdist} shows the two distributions. Removal reaches the threshold in fewer steps than injection under either objective. The mismatched objective, which reproduces the original protocol, gives a lower median $R$ than the matched one, so the earlier $R > 1$ does not come from the loss used in each direction.

\begin{table}[htbp]
\small\sf\centering
\caption{Asymmetry ratio $R$ under the two objectives on the same 180-condition grid ($\theta = 0.7$). Both medians lie below one.}
\label{tab:ab}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcc}
\toprule
\textbf{Quantity} & \textbf{Matched} & \textbf{Mismatched} \\
\midrule
Converged conditions & 168 & 119 \\
Injection-censored (\%) & 0.0 & 27.2 \\
Median $R$ & 0.393 & 0.074 \\
95\% CI (model-clustered) & [0.359, 0.426] & [0.056, 0.080] \\
Share with $R>1$ & 0.190 & 0.067 \\
Wilcoxon $\log R$ ($p$) & $4\times10^{-15}$ & $2\times10^{-19}$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{images/figure_R_distribution.png}
\caption{Distribution of the asymmetry ratio $R$ under the matched and mismatched objectives, on a $\log_{10}$ scale. The dashed line marks $R = 1$. Both distributions concentrate below one.}
\label{fig:rdist}
\end{figure}

\subsection{Censoring accounts for the reported asymmetry}

The two objectives differ sharply in one respect. Under the matched objective, injection reaches $\theta$ in every condition that starts below it, so no condition is injection-censored. Under the mismatched objective, cross-entropy injection often fails to reach $\theta$ within the step budget, so about a quarter of conditions carry no defined ratio. In the original protocol these conditions did not drop out. A censored injection sets $T_{\mathrm{bias}}$ to its cap and a censored removal sets $T_{\mathrm{debias}}$ to its cap, so the pair returns a fixed ceiling ratio that the mean then averages in.

Figure~\ref{fig:censoring} shows the effect. The defined ratios all sit below one, and their median stays far below one. The censored conditions form a mass at the ceiling. A mean taken over both groups is pulled above one, which is the reported asymmetry. The mean reflects the ceiling mass, not the measured ratios. Once censored conditions are excluded and the median replaces the mean, the asymmetry disappears.

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{images/figure_censoring.png}
\caption{Under the mismatched objective, defined ratios sit below one, but censored conditions form a ceiling mass that lifts the mean above one while the median stays low.}
\label{fig:censoring}
\end{figure}

\subsection{The pattern holds across models and languages}

Table~\ref{tab:permodel} reports $R$ by model under the matched objective. No model has a median $R$ above one. The medians range from 0.297 for mmBERT to 0.440 for XLM-RoBERTa. The share of conditions with $R > 1$ stays a minority for every model, from 3\% for IndicBERTv2 to 36\% for XLM-RoBERTa. The two models that showed the strongest apparent hysteresis under a mismatched objective, mmBERT and XLM-RoBERTa, now sit at opposite ends of this range, so the earlier ordering does not carry over to the controlled measurement.

\begin{table}[htbp]
\small\sf\centering
\caption{Matched-objective ratio $R$ by model, over converged conditions ($\theta = 0.7$). No model shows a median above one.}
\label{tab:permodel}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{$n$} & \textbf{Median $R$} & \textbf{$R>1$} & \textbf{Share $R>1$} \\
\midrule
mBERT & 33 & 0.392 & 9 & 0.273 \\
XLM-RoBERTa & 33 & 0.440 & 12 & 0.364 \\
MuRIL & 33 & 0.426 & 3 & 0.091 \\
IndicBERTv2 & 33 & 0.407 & 1 & 0.030 \\
mmBERT & 36 & 0.297 & 7 & 0.194 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:perlang} reports $R$ by language. English shows the highest median at 0.629, above Hindi at 0.333 and Bengali at 0.409, yet all three medians remain below one. The English result is closest to symmetry, consistent with the earlier observation that English biases behave differently from Hindi and Bengali, but even the English median does not cross one. The language ordering survives the control while the direction of the asymmetry does not.

\begin{table}[htbp]
\small\sf\centering
\caption{Matched-objective ratio $R$ by language, over converged conditions ($\theta = 0.7$).}
\label{tab:perlang}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcccc}
\toprule
\textbf{Language} & \textbf{$n$} & \textbf{Median $R$} & \textbf{$R>1$} & \textbf{Share $R>1$} \\
\midrule
English & 54 & 0.629 & 14 & 0.259 \\
Hindi & 54 & 0.333 & 5 & 0.093 \\
Bengali & 60 & 0.409 & 13 & 0.217 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:percat} reports $R$ by bias category. Three of the four categories, race-colour, religion, and nationality, keep a median below one. Socioeconomic status is the exception: its median sits above one, and most of its conditions cross one. The category effect does not lift the whole grid, because the overall median and every model and language median stay below one. It does show that the direction can flip for a particular category, so a category-level claim needs its own check rather than the grid average.

\begin{table}[htbp]
\small\sf\centering
\caption{Matched-objective ratio $R$ by bias category. Only socioeconomic status has a median above one.}
\label{tab:percat}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcccc}
\toprule
\textbf{Category} & \textbf{$n$} & \textbf{Median $R$} & \textbf{$R>1$} & \textbf{Share $R>1$} \\
\midrule
Nationality & 39 & 0.245 & 1 & 0.026 \\
Race-colour & 45 & 0.378 & 4 & 0.089 \\
Religion & 45 & 0.376 & 2 & 0.044 \\
Socioeconomic & 39 & 1.409 & 25 & 0.641 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The result is stable across the three random seeds. Table~\ref{tab:perseed} reports the matched-objective median $R$ for each seed. The three medians fall in a narrow band, so the finding does not depend on a single initialisation.

\begin{table}[htbp]
\small\sf\centering
\caption{Matched-objective ratio $R$ by seed, showing stability across the three runs.}
\label{tab:perseed}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcccc}
\toprule
\textbf{Seed} & \textbf{$n$} & \textbf{Median $R$} & \textbf{$R>1$} & \textbf{Share $R>1$} \\
\midrule
42 & 56 & 0.373 & 10 & 0.179 \\
123 & 56 & 0.376 & 10 & 0.179 \\
456 & 56 & 0.447 & 12 & 0.214 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\subsection{Gradient norms set the direction}

Removal being the faster direction has a simple mechanical cause. Removal starts from the injected checkpoint, where the stereotypical and anti-stereotypical log-probabilities differ by a large margin, so the signed gap $\delta$ is large and its gradient is large. Injection starts from a near-neutral checkpoint, where the gap is small and its gradient is small. Larger gradients move the score faster. The curves in Figure~\ref{fig:curves} already showed this: the injection line climbs slowly, while the removal line falls back below the threshold in fewer steps. The gradient $L_2$ norm during removal exceeds that during injection by a median factor of 1.115, and a Wilcoxon signed-rank test on the paired norms confirms the difference. The step count therefore encodes the gradient scale of each starting state. The ratio compares a fast reverse path against a slow forward path, and that comparison is set by where each path begins, not by any resistance in the loss landscape.

\subsection{The ratio depends on the threshold}
\label{sec:thetasweep}

If the ratio measured a property of the model, it would not move much when the threshold moves. It moves a great deal. Table~\ref{tab:theta} sweeps $\theta$ across the plausible range, using the two categories at the ends of the earlier per-category spread. The median ratio falls from well above one at the low threshold to well below one at the high threshold, a swing of more than an order of magnitude on the same models and data. Figure~\ref{fig:theta} shows the same trend for both categories.

The cause is the one seen in Section~\ref{sec:results}. At a low threshold, injection crosses in very few steps, so $T_{\mathrm{bias}}$ is tiny and the ratio inflates. At a high threshold, injection struggles to reach the mark, so $T_{\mathrm{bias}}$ is large and the ratio shrinks. The ratio tracks where the threshold sits relative to the range of bias the model can reach, not any asymmetry between acquisition and removal. A single reported $R$ is therefore a reading at one arbitrary threshold, and a different threshold can reverse the story.

\begin{table}[htbp]
\small\sf\centering
\caption{Median ratio $R$ against threshold $\theta$, over converged conditions. The ratio swings by more than an order of magnitude across the range.}
\label{tab:theta}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lccc}
\toprule
\textbf{$\theta$} & \textbf{$n$} & \textbf{Median $R$} & \textbf{Reading} \\
\midrule
0.60 & 48 & 5.11 & removal looks much harder \\
0.70 & 84 & 0.60 & removal looks faster \\
0.80 & 28 & 0.13 & removal looks much faster \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{images/figure_theta_sensitivity.png}
\caption{Median ratio $R$ against threshold $\theta$ for two bias categories, on a log scale. The dashed line marks $R = 1$. The ratio crosses from above one to below one as the threshold rises.}
\label{fig:theta}
\end{figure}

\subsection{A threshold-free measure: the hysteresis loop}
\label{sec:loop}

Because the ratio depends on the threshold, a fair test of hysteresis should use no threshold and no step count. The loop measure sweeps a control field $\lambda \in [0,1]$ that mixes the two training pressures through one functional, $L(\lambda) = (1 - 2\lambda)\,\delta$: at $\lambda = 1$ the field opens the gap, at $\lambda = 0$ it closes the gap, and $\lambda = 0.5$ applies no net pressure. The field is raised from 0 to 1 and then lowered back to 0 with one optimiser carried across the whole sweep, and the bias score is recorded at each stage. If the downward path retraces the upward path, the state has no memory and there is no hysteresis. If the paths differ, the enclosed loop has a non-zero signed area $A = \oint B \, d\lambda$, which measures path dependence directly.

Table~\ref{tab:loop} reports the loop area over 135 conditions. The median area is small, near one to two per cent of a full loop, and most conditions are positive. A sign-flip permutation test, which assigns each condition a random sign under the null of no loop, rejects zero. Figure~\ref{fig:loop} shows a large loop and a near-flat one side by side. The effect is not uniform across models: it is clearest for IndicBERTv2 and MuRIL and near zero for mmBERT. A small, model-dependent hysteresis therefore remains once the ratio artefact is removed. It is real but small, and nowhere near the large asymmetry that the reported ratio implied.

\begin{table}[htbp]
\small\sf\centering
\caption{Hysteresis loop area $A$ over 135 conditions, and by model. The loop is small and positive on average, and largest for IndicBERTv2 and MuRIL.}
\label{tab:loop}
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lcc}
\toprule
\textbf{Group} & \textbf{$n$} & \textbf{Median $A$} \\
\midrule
All conditions & 135 & 0.016 \\
\midrule
IndicBERTv2 & 27 & 0.103 \\
MuRIL & 27 & 0.067 \\
XLM-RoBERTa & 27 & 0.020 \\
mBERT & 27 & 0.010 \\
mmBERT & 27 & $-0.001$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{images/figure_loop_area.png}
\caption{Bias score against the control field $\lambda$ for two conditions. The left panel shows a clear loop where the downward path stays above the upward path; the right panel shows a near-flat loop.}
\label{fig:loop}
\end{figure}

\section{Discussion}
\label{sec:discussion}

The step-count ratio $R$ has been read as evidence that bias removal is intrinsically harder than bias injection. That reading does not survive three checks. Under a matched objective the median ratio falls below one, so removal is the faster direction. The earlier value above one came from censored conditions kept at their ceiling, which lift the mean while the median stays low. Most telling, the ratio swings by more than an order of magnitude when the threshold moves across its plausible range, from well above one to well below one on the same models and data. A quantity that reverses with an arbitrary threshold choice cannot be a stable property of the model.

A threshold-free measure gives the honest picture. The hysteresis loop, measured by sweeping a control field with no step count, has a small positive area that a permutation test separates from zero. The effect is real but small, near one to two per cent of a full loop, and it is concentrated in some models rather than shared by all. So bias dynamics are not perfectly reversible, but the asymmetry is minor, not the large effect a reported ratio above one suggests.

The finding matters for how debiasing budgets are set. A pipeline that allocates extra compute to removal on the basis of a reported ratio above one may be responding to a measurement artefact. The safer practice is to exclude censored conditions rather than cap them, to report the median rather than the mean, to state the objective used in each direction, and to prefer a threshold-free measure when the claim is about path dependence. The result sits with a wider concern in the fairness literature, where static bias scores shift with the metric and the probing choice and predict downstream bias poorly. The step-count ratio is a dynamic cousin of those scores, and it shows the same weakness: a reasonable-looking number can move for reasons that have nothing to do with the model.

\subsection{Limitations}

This study has clear limits. It covers encoder models only, each adapted with a rank-16 low-rank adapter, so full fine-tuning and generative models are not tested and may behave differently. It reports four bias categories with adequate sample counts, and one of them, socioeconomic status, has a median ratio above one under the matched objective, which shows the direction can depend on the category. The bias score is one choice among several, and a different score could change the crossing pattern. The threshold sweep and the loop-area measure both point past these limits, but the loop-area sweep is trained to a fixed budget per field step, so a longer budget could sharpen the small effect it detects. Full fine-tuning is the main remaining check, and the code for it accompanies the paper.

\subsection{Reproducibility}

The five encoder models are mBERT, XLM-RoBERTa, MuRIL, IndicBERTv2, and mmBERT, each adapted with a low-rank adapter of rank 16 that starts from the pretrained weights. Injection and removal share one optimiser, a learning rate of $2\times10^{-4}$, and one schedule. The bias score is read every 25 steps; injection runs for at most 500 steps and removal for at most 1{,}000. Each condition uses one of three seeds. Experiments ran on a single 24\,GB GPU. The bias benchmarks, \textit{Multi-CrowS-Pairs} and the \textit{Indian-BhED}-derived Indian set, are extended to Hindi and Bengali by translation with back-translation checks, and cover caste, gender, religion, race, and the Western-framed categories. The code and per-condition results accompany the paper, so every table and figure can be regenerated from the stored outputs.

\section{Conclusion}
\label{sec:conclusion}

This work tested a reported asymmetry between bias injection and bias removal on one grid of five multilingual encoders and three languages. Three checks show that the step-count ratio does not measure bias hysteresis. Under a matched objective the median ratio falls below one, so removal is the faster direction. The earlier value above one traces to censored conditions kept at a ceiling, which lift the mean while the median stays low. The ratio also swings by more than an order of magnitude when the threshold moves across its range, so it cannot be a stable property of the model. A threshold-free loop-area measure gives the honest picture: a small positive loop, near one to two per cent of full scale, concentrated in some models. Bias dynamics carry a minor path dependence, not the large asymmetry the ratio implied. The main remaining check is full fine-tuning, whose code accompanies the paper. An asymmetry claim about bias dynamics needs a matched objective, censoring-aware and median-based analysis, and a threshold-free measure before it can stand.

\begin{acks}
The author used a generative-AI coding assistant to help implement the experiment scripts and to draft and edit parts of the manuscript text. All code was reviewed and tested by the author, and all reported numbers were read from the experiment output files. The author takes full responsibility for the content.
% TODO(koushik): confirm exact wording of AI-use disclosure per current Sage policy.
\end{acks}

\section*{Statements and Declarations}

\subsection*{Ethical considerations}
This study uses only publicly available pretrained models and published bias benchmarks. It involves no human participants, human data, or human tissue, and required no ethics approval.

\subsection*{Consent to participate}
Not applicable.

\subsection*{Consent for publication}
Not applicable.

\subsection*{Declaration of conflicting interests}
The author declared no potential conflicts of interest with respect to the research, authorship, and publication of this article.

\subsection*{Funding}
This research received no specific grant from any funding agency in the public, commercial, or not-for-profit sectors.

\subsection*{Data availability}
The code, per-condition results, and the multilingual bias benchmarks that support the findings of this study are available in a public repository. To preserve anonymity during review, the repository and dataset identifiers are given on the separate title page and will be made openly available on acceptance.

\subsection*{Author contributions}
The sole author conceived the study, implemented the experiments, analysed the results, and wrote the manuscript.

\bibliographystyle{SageH}
\bibliography{sample}

\end{document}
