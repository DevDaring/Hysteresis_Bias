# ============================================================
# REQUIRES: Python 3.12+
# Flash Attention 2 requires Python 3.12 and CUDA 12.4
#
# INSTALL ORDER (GCP / Linux):
#   Step 1: pip + torch with CUDA 12.4
#     python3 -m pip install --upgrade pip setuptools wheel
#     python3 -m pip install torch==2.5.1 torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu124
#
#   Step 2: Flash Attention (pre-built wheel for Python 3.12 + CUDA 12 + Torch 2.5)
#     wget -q https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.5cxx11abiFALSE-cp312-cp312-linux_x86_64.whl -O /tmp/flash_attn.whl
#     python3 -m pip install --no-deps /tmp/flash_attn.whl
#
#   Step 3: All other dependencies
#     pip install -r requirements.txt --break-system-packages
# ============================================================

# --- Core (versions tested on GCP H200 with CUDA 12.4) ---
# NOTE: torch is installed separately in Step 1 (not listed here)
transformers>=4.49.0,<5.0.0
peft>=0.10.0
datasets==2.16.0
accelerate==0.34.0
bitsandbytes==0.46.1
safetensors>=0.4.0
sentencepiece==0.2.0
protobuf==4.25.0

# Data
pandas==2.2.2
numpy<2.0

# Evaluation & Analysis
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
pyyaml>=6.0
python-dotenv==1.0.0
tqdm==4.65.0
huggingface-hub>=0.21.0
requests==2.31.0

# Parallel execution & process management
psutil>=5.9.0           # CPU/RAM monitoring for parallel runs
filelock>=3.13.0        # File locking for concurrent result writes

# GPU monitoring (optional but recommended for H200)
nvidia-ml-py>=12.535.0  # Python bindings for nvidia-smi (NVML)

# Flash Attention 2
# NOTE: Installed separately via pre-built wheel (see Step 2 above)
# flash-attn==2.8.3

# Hessian computation [7]
# NOTE: Implemented via power iteration in src/analysis/hessian_analysis.py
# No external dependency needed
